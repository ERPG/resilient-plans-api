framework:
    cache:
        # `tags: true` wraps the pool in TagAwareAdapter (required for invalidateTags). Prod overrides to Redis.
        pools:
            search.cache:
                adapter: cache.adapter.array
                tags: true
