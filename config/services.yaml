parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/Kernel.php'

    App\Application\Provider\PlanProvider: '@App\Infrastructure\Provider\Http\HttpPlanProvider'

    App\Infrastructure\Provider\Http\HttpPlanProvider:
        arguments:
            $baseUrl: '%env(PROVIDER_BASE_URL)%'
            $timeout: '%env(float:PROVIDER_TIMEOUT)%'
